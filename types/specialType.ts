export interface TSpecial {
  id?: string;
  name: string;
  hero?: string;
  uri: string;
  length: number;
  make: string;
  cabins: number;
  sleeps: number;
  regularPrice: number;
  sellPrice: number;
  createdAt?: string;
  updatedAt?: string;
}
